name: kwon_mediapipe_landmarker_example
description: "Demonstrates how to use the kwon_mediapipe_landmarker plugin."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  kwon_mediapipe_landmarker:
    path: ../
  camera: ^0.11.0+2
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
